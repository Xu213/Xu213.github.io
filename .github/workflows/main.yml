name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Join HEXO Condition
      run: npm install hexo
    - name: Bulid To Public
      run: |
        pwd
